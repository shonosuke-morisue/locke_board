# 超人ロック ボードゲーム - CLAUDE.md

## ビルド・開発サーバーの起動コマンド

### サーバー（バックエンド）

```bash
cd server
npm install
npm run dev      # 開発サーバー起動（ts-node-dev、ポート3001）
npm run build    # TypeScriptビルド（dist/へ出力）
npm run start    # ビルド済みファイルを起動
```

### クライアント（フロントエンド）

```bash
cd client
npm install
npm run dev      # 開発サーバー起動（Vite、ポート5173）
npm run build    # プロダクションビルド（dist/へ出力）
npm run preview  # ビルド済みファイルをプレビュー
```

### 同時起動（推奨）

ターミナルを2つ開いて、それぞれでサーバーとクライアントを起動してください。

```
# ターミナル1
cd server && npm install && npm run dev

# ターミナル2
cd client && npm install && npm run dev
```

ブラウザで `http://localhost:5173` にアクセスします。

### 修正後のサーバー再起動

コードを修正した後は必ずサーバーを手動で再起動してください。
ts-node-dev はファイル保存のたびに再コンパイルするため、複数ファイルを連続編集すると中間状態でコンパイルエラーになることがあります。

```bash
# 3001番ポートを解放してサーバーを再起動
kill $(lsof -ti:3001) && cd server && npm run dev
```

クライアント（Vite）はホットリロードで自動更新されますが、プロセスが落ちた場合は再起動が必要です。

---

## 本番環境（Render）へのデプロイ

ルートの `package.json` にビルド・起動スクリプトが定義されている。ビルド時は `--include=dev` オプションが必要（Viteなどの devDependencies を使うため）。

本番環境では `NODE_ENV=production` を設定して起動する。このとき：
- サーバーが `client/dist` を静的ファイルとして配信する（SPA対応）
- クライアントは同一オリジンのサーバーに接続する（開発時は `hostname:3001` に接続）

---

## アーキテクチャの概要

```
locke_board/
├── package.json              # ルートのビルド・起動スクリプト（Render用）
├── client/                   # Reactフロントエンド（Vite + TypeScript）
│   ├── src/
│   │   ├── App.tsx           # ルートコンポーネント（フェーズ切り替え）
│   │   ├── constants.ts      # 共通定数（PLANET_NAMES, ROW_LABELS, KEY_POINT_LABELS）
│   │   ├── hooks/
│   │   │   ├── useSocket.ts  # Socket.io接続とゲーム状態管理
│   │   │   ├── useTouchTap.ts  # タップ操作（シングル/ダブル/2本指）カスタムフック
│   │   │   └── useTouchDrag.ts # タッチドラッグ&ドロップ カスタムフック
│   │   ├── types/
│   │   │   └── game.ts       # 型定義
│   │   └── components/
│   │       ├── Lobby.tsx         # ロビー画面
│   │       ├── FactionSetup.tsx  # 陣営割り当て画面
│   │       ├── AmbushSetup.tsx   # 待ち伏せ設定画面
│   │       ├── Board.tsx         # 惑星編ゲームボード（6×7）
│   │       ├── Card.tsx          # 惑星編カードコンポーネント
│   │       ├── CardDetail.tsx    # カード詳細ポップアップ（現在未使用）
│   │       ├── BaseSetup.tsx     # 重要拠点設定画面
│   │       ├── BaseBoard.tsx     # 秘密基地編ゲームボード（6×6）
│   │       ├── BaseCard.tsx      # 秘密基地カードコンポーネント
│   │       └── PlayerPiece.tsx   # プレイヤーコマ
│
└── server/                   # Node.jsバックエンド（Express + Socket.io）
    └── src/
        ├── index.ts          # サーバーエントリーポイント
        ├── types.ts          # 型定義
        ├── gameState.ts      # ゲーム状態管理
        └── socketHandlers.ts # Socket.ioイベントハンドラー
```

### 設計方針

- **シングルルーム**: サーバーは1つのゲームセッションのみ管理
- **秘密情報フィルタリング**: サーバーが各クライアントの陣営に応じてゲーム状態をフィルタリングして送信
- **リアルタイム同期**: カード操作・コマ移動は即座に全プレイヤーに反映
- **外部UIライブラリ不使用**: スタイルはインラインスタイルのみ
- **プレイヤーID永続化**: `localStorage` にプレイヤー情報を保存し、リロード時に自動再接続

---

## ゲームフェーズの説明

### LOBBY
- プレイヤーが名前を入力して参加申請
- ホスト（最初に参加したプレイヤー）が各参加者を承認
- 2人以上承認されるとホストがゲーム開始可能
- **参加キャンセル**: LOBBYフェーズ中のみ退出できる。他のフェーズでは退出不可
- **ホスト移譲（退出時）**: 「承認済み＆接続中」→「接続中」→「リスト先頭」の優先順位で次のプレイヤーにホスト権が移譲される
- **ホスト移譲（切断時）**: 接続が失われた場合も接続中の次のプレイヤーに自動移譲される（プレイヤーはリストに残る）
- **プレイヤー数上限**: 最大10人まで参加可能

### FACTION_SETUP
- ホストが各プレイヤーに陣営を割り当てる
  - `good`（秩序）: ロックの味方側
  - `evil`（混沌）: ロックの敵側
- evilプレイヤーが1人以上必要
- 全承認済みプレイヤーへの割り当て完了後、ホストが次フェーズへ

### AMBUSH_SETUP
- evilプレイヤーがボード上の2箇所を「待ち伏せ」に設定
- 待ち伏せの位置はgoodプレイヤーには通常の伏せカードに見える
- evilプレイヤーには待ち伏せマスが視覚的にわかるよう表示
- クリックのたびに即座にサーバーへ送信・全evilプレイヤーにリアルタイム同期される
- 2箇所設定完了後、evilプレイヤーがゲーム開始を押す
- 待ち伏せマスにはラベル「A」「B」が付与される（1箇所目がA、2箇所目がB）

### PLAYING
- 通常のゲームプレイ
- goodプレイヤーのみ宇宙港（列0）に配置された状態でスタート（evilはコマなし）
- コマのドラッグ&ドロップによる移動（マウス・タッチ対応）
- カードのダブルクリック／ダブルタップで表裏切り替え
- 表向きカードのクリックで右側パネルに詳細表示
- 裏カードをめくると自動的に詳細パネルが開く
- 詳細パネルはボードグリッドの右側（幅200px）に常時表示される
- ホストはいつでもリスタート（プレイヤー維持）またはゲーム終了（全リセット）が可能
- **除外ゾーン**: ボード下部のドラッグ&ドロップエリアにコマを移動すると除外扱いになる（座標 `{ row: -1, col: -1 }`）
- **秘密基地編へ移行**: ホストが「秘密基地編へ」ボタンを押すと BASE_SETUP フェーズへ遷移する

### BASE_SETUP
- ホストが PLAYING フェーズ中に「秘密基地編へ」ボタンを押すと遷移する
- 全プレイヤーのコマが除外ゾーン（`{ row: -1, col: -1 }`）にリセットされる
- evilプレイヤーが秘密基地ボードの内側16マス（B2〜E5、row 1〜4 / col 1〜4）に重要拠点を4箇所設定する
- 最外周（A行・F行・1列・6列）は選択不可
- 設定順に「エネルギー・ルーム」「コンピューター・ルーム」「研究室」「指令室」のラベルが付与される
- クリックのたびに即座にサーバーへ送信・全evilプレイヤーにリアルタイム同期される
- goodプレイヤーには待機画面が表示される
- 4箇所設定完了後、evilプレイヤーが「設定完了」ボタンを押すと BASE_PLAYING フェーズへ遷移

### BASE_PLAYING
- 秘密基地編のゲームプレイ
- 6行×6列の秘密基地ボードを使用（行ラベル A〜F、列ラベル 1〜6）
- コマのドラッグ&ドロップによる移動（マウス・タッチ対応）
- カードのダブルクリック／ダブルタップで表裏切り替え
- 表向きカードのクリックで右側パネルに詳細表示
- 裏カードをめくると自動的に詳細パネルが開く
- **カード破壊**: 表向きカードを右クリック（モバイルは2本指タッチ）するとコンテキストメニューが開き、「破壊」または「元に戻す」を選択できる
- 重要拠点カードの表示は evil には常に公開、good にはカードが表になった時のみ公開
- **除外ゾーン**: PLAYING フェーズと同じ仕様
- ホストはいつでもリスタートまたはゲーム終了が可能

---

## Socket.ioイベント一覧

### クライアント → サーバー

| イベント名 | データ | 説明 | 権限 |
|---|---|---|---|
| `player:join` | `{ name: string, playerId: string }` | ゲームに参加・再接続（playerId はlocalStorageで永続化） | 全員 |
| `player:leave` | なし | LOBBYフェーズ中に退出（他フェーズでは不可） | 全員 |
| `player:approve` | `{ playerId: string }` | プレイヤーを承認 | ホストのみ |
| `faction:assign` | `{ playerId: string, faction: 'good' \| 'evil' }` | 陣営を割り当て | ホストのみ |
| `faction:done` | なし | 陣営割り当て完了（LOBBYなら開始、FACTION_SETUPなら完了） | ホストのみ |
| `ambush:set` | `{ positions: [{row, col}, ...] }` | 待ち伏せ位置を設定（0〜2箇所、クリックのたびに送信） | evilのみ |
| `ambush:done` | なし | 待ち伏せ設定完了（2箇所設定済みの場合のみ有効） | evilのみ |
| `piece:move` | `{ playerId: string, row: number, col: number }` | コマを移動（除外ゾーンは row=-1, col=-1） | 全員 |
| `card:flip` | `{ row: number, col: number }` | 惑星編カードの表裏切り替え | 全員 |
| `game:restart` | なし | ゲームをリスタート（プレイヤー維持・ボードリセット） | ホストのみ |
| `game:end` | なし | ゲーム終了（全プレイヤーリセット・参加画面に戻る） | ホストのみ |
| `game:startBase` | なし | 秘密基地編へ移行（PLAYINGフェーズからのみ有効） | ホストのみ |
| `keyPoint:set` | `{ positions: [{row, col}, ...] }` | 重要拠点位置を設定（0〜4箇所、クリックのたびに送信） | evilのみ |
| `keyPoint:done` | なし | 重要拠点設定完了（4箇所設定済みの場合のみ有効） | evilのみ |
| `baseCard:flip` | `{ row: number, col: number }` | 秘密基地カードの表裏切り替え | 全員 |
| `baseCard:destroy` | `{ row: number, col: number }` | 秘密基地カードを破壊状態にする | 全員 |
| `baseCard:restore` | `{ row: number, col: number }` | 秘密基地カードの破壊状態を解除する | 全員 |

### サーバー → クライアント

| イベント名 | データ | 説明 |
|---|---|---|
| `game:state` | `GameState` | ゲーム状態（陣営に応じてフィルタリング済み） |
| `error` | `{ message: string }` | エラー通知 |

---

## ゲームボード構成

- 6行 × 7列のグリッド
- 列0: 「宇宙港」マス（ゲーム開始時の配置場所・カードなし）
- 列1〜6: 伏せ状態のカードが配置される（計36枚）

### 行ごとの地名

| 行 | 地名 | 宇宙港名 |
|---|---|---|
| 1 | 地球 | 地球宇宙港 |
| 2 | ロンウォール | ロンウォール宇宙港 |
| 3 | セレン | セレン宇宙港 |
| 4 | トア | トア宇宙港 |
| 5 | ディナール | ディナール宇宙港 |
| 6 | マイア | マイア宇宙港 |

マス名は「地名 + 列番号」（例: 地球1、マイア6）。地名は `client/src/constants.ts` の `PLANET_NAMES` で一元管理。

### カードデッキ構成

全78枚のデッキをシャッフルし、先頭36枚をボードに配置する。

#### 能力カード（計26枚）
`isAbility: true` のカード。開いたプレイヤーのみ名称・詳細を閲覧可能。他のプレイヤーには「能力カード」と表示される。能力カードかどうかの判定はカード名ではなく `isAbility` フラグで行う。

| カード名 | 枚数 |
|---|---|
| [能力]巡洋艦 | 1 |
| [能力]手下 | 2 |
| [能力]ＥＳＰジャマーLv3〜5 | 各1 |
| [能力]ＥＳＰフィールドLv3〜5 | 各1 |
| [能力]エネルギースーツ | 1 |
| [能力]個人用パワードスーツLv4〜5 | 各1 |
| [能力]ニケ | 1 |
| [能力]亜空間フィールド | 1 |
| [能力]ＥＳＰコントローラー | 1 |
| [能力]エネルギー吸収ボールLv3〜5 | 各1 |
| [能力]クローン | 3 |
| [能力]ジオイド弾 | 1 |
| [能力]変身 | 1 |
| [能力]ラフノールの鏡Lv5〜7 | 各1 |

#### イベントカード（計52枚）

| カード名 | 枚数 |
|---|---|
| 開拓地 | 2 |
| 歓楽街 | 2 |
| 工業地域 | 2 |
| 住宅街 | 2 |
| スラム街 | 2 |
| 宇宙港 | 6 |
| 逮捕 | 4 |
| 戦闘発生 | 10 |
| トラップ | 3 |
| 情報入手 | 13 |
| 自分の正体露顕 | 2 |
| 他人の正体判明 | 4 |

### 秘密情報フィルタリング仕様

サーバーがプレイヤーごとにゲーム状態をフィルタリングして送信する。

| 情報 | good | evil |
|---|---|---|
| 待ち伏せマスの位置 | 非公開（伏せカードに見える） | 公開 |
| 待ち伏せマスの `isAmbush` フラグ | カードが表になった時のみ `true` | 常に `true` |
| 能力カードの名称・詳細（開いた本人） | 公開 | 公開 |
| 能力カードの名称・詳細（他人が開いた） | `name` と `content` が空文字で送信 | 同左 |

`openedBy` フィールドは、カードを**最初に表にしたプレイヤーのID**を記録する。一度設定されたら変更されない（裏返して再度開いても更新されない）。

---

## 秘密基地編ボード構成

- 6行 × 6列のグリッド（行ラベル A〜F、列ラベル 1〜6）
- 全マスにカードが配置される（宇宙港マスなし）
- 行・列ラベルは `client/src/constants.ts` の `ROW_LABELS` で一元管理

### 重要拠点の仕様

- 内側16マス（B2〜E5 / row 1〜4、col 1〜4）にのみ配置可能
- 設定順に以下のラベルが付与される（`KEY_POINT_LABELS` で管理）
  1. エネルギー・ルーム
  2. コンピューター・ルーム
  3. 研究室
  4. 指令室
- カードデータ上は `isKeyPoint: true` と `keyPointLabel` フィールドで管理される

### カードデッキ構成

全52枚のデッキをシャッフルし、先頭36枚をボードに配置する（残り16枚は使用しない）。

| カード名 | 枚数 |
|---|---|
| ESPコントローラーシステム | 1 |
| E.K. | 1 |
| エネルギー吸収体 | 3 |
| オーディオルーム | 1 |
| カーンの聖母 | 2 |
| 喫茶店「ダリア」 | 1 |
| キング編集室 | 1 |
| 警戒装置 [1-1] | 2 |
| 警戒装置 [1-2] | 1 |
| 警戒装置 [2-1] | 1 |
| 警戒装置 [2-2] | 1 |
| 警戒装置 [3-2] | 1 |
| 警戒装置 [3-3] | 1 |
| 警戒装置 [4-3] | 1 |
| 警戒装置 [4-4] | 1 |
| 警戒装置 [5-4] | 1 |
| 化粧室 | 1 |
| 幻覚の部屋[4] | 1 |
| 幻覚の部屋[6] | 1 |
| サイン会場 | 1 |
| 重積ヴォーティクス | 1 |
| 通路 | 10 |
| トラップ | 2 |
| ノヴァ（新星）| 1 |
| ブラックホール | 1 |
| 兵員室 | 3 |
| ホワイトホール | 1 |
| 山羊牧場 | 1 |
| ランダムテレポート | 8 |

秘密基地カードはすべて `isAbility: false`。能力カード秘匿ロジックは適用されない。

### 秘密情報フィルタリング仕様（秘密基地編）

| 情報 | good | evil |
|---|---|---|
| 重要拠点カードの `isKeyPoint` フラグ | カードが表になった時のみ `true` | 常に `true` |
| 重要拠点カードの `keyPointLabel` | カードが表になった時のみ公開 | 常に公開 |

### カード破壊状態

- `isDestroyed: true` のカードは破壊扱いとなり、視覚的に区別して表示される
- 破壊・復元は全員がコンテキストメニューから操作可能（右クリック / 2本指タッチ）
- 破壊状態は詳細パネルにも反映される
