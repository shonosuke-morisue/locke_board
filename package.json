{
  "name": "locke-board",
  "version": "0.1.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd client && npm install --include=dev && npx vite build && cd ../server && npm install --include=dev && npm run build",
    "start": "NODE_ENV=production node server/dist/index.js"
  }
}
