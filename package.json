{
  "name": "locke-board",
  "version": "0.1.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd client && npm install && ./node_modules/.bin/vite build && cd ../server && npm install && npm run build",
    "start": "NODE_ENV=production node server/dist/index.js"
  }
}
